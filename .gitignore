# Python
__pycache__/
*.pyc

# Virtual environment
.venv/

# Environment / secrets
.env
!.env.example
# token files
*token*.txt
*.token

# Scratch / temp
/tmp/

# Terraform (state + real tfvars are local; .example is committed)
**/.terraform/
*.tfstate
*.tfstate.*
**/terraform.tfvars
crash.log

# Local-only working log (session/evidence notes with real identifiers — not for the repo)
docs/reference/vcl_architecture_and_evidence.md

# Separate context-engineering tooling project — copied per-lesson, not part of this repo
manageskills/

# IntelliJ / IDE
.idea/
*.iml

# Lock files
*.lock
uv.lock

# infra generated IDs (project-number resource paths; not secrets but not for public repo)
infra/channels.env

# Publication — local agent tooling + authored instructions (untracked, kept on disk)
AGENTS.md
CLAUDE.md
GEMINI.md
.claude/
.agent/
docs/archive/
